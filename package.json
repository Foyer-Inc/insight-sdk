{
  "name": "insight-sdk",
  "version": "1.0.0",
  "description": "SDK for connecting to Foyer's Insight service(s)",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "dependencies": {
    "@types/node-fetch": "^2.5.8",
    "jimp": "^0.16.1",
    "node-fetch": "^2.6.1",
    "typescript": "^4.4.2"
  },
  "devDependencies": {},
  "scripts": {
    "prepare": "rm -rf ./build; tsc --build tsconfig.json",
    "build": "rm -rf ./build; tsc --build tsconfig.json",
    "start_local": "rm -rf ./build; tsc --build tsconfig.json; node ./build/example/example.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Foyer Inc.",
  "license": "ISC"
}
